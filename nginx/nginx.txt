/etc/nginx/sites-available

/etc/nginx/sites-available/app_gateway : 
./app_gateway

sudo cp ./app_gateway //etc/nginx/sites-available/app_gateway
sudo ln -s /etc/nginx/sites-available/app_gateway /etc/nginx/sites-enabled/app_gateway

# sudo service nginx stop
# install nginx --with-debug
sudo rm /etc/nginx/sites-enabled/default
sudo service nginx restart

sudo mkdir -p /etc/nginx/ssl
sudo openssl req -x509 -nodes -days 1095 -newkey rsa:2048 -keyout /etc/nginx/ssl/nginx.key -out /etc/nginx/ssl/nginx.crt
